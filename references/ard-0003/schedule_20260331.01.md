# Schedule (2026-03-31)

## 작업 순서 (ard-0003)

```text
4.1          →  4.2                    →  4.3              →  6.2 선행 확정 후
Idempotency     lazy mode                DomainCollection     v1.4.x
Key             + diff Worker            + saveAll(batch)     UIComposer + UILayout

v1.1.x       →  v1.2.x               →  v1.3.x           →  v1.4.x

       →  v1.5.x                 →  v1.6.x           →  v2.0.0
          디버깅 + 주석 정비           WebDoc rebuild        --allow-empty 릴리즈
```

## 브랜치 전략 요약

| 버전 | 브랜치명 | 타입 |
|---|---|---|
| v1.1.x | `feature/idempotency-key` | feature |
| v1.2.x | `feature/lazy-tracking-mode` | feature |
| v1.3.x | `feature/domain-collection` | feature |
| v1.4.x | `feature/ui-composer` | feature |
| v1.5.x | `chore/debug-polish` | chore |
| v1.6.x | `chore/webdoc-rebuild` | chore |
| v2.0.0 | `release/v2.0.0` | release |

## 선행 조건

- **v1.4.x 착수 전:** `ard-0003-alignment.md § 6.2` `readonlyTemplateSelector` 결정 완료 상태 확인
- **v1.4.x 완료 후:** `§ 6.3` `performance.measure()` 실측 → guard 수치 확정 후 v1.5.x 진입
- **v2.0.0:** `--allow-empty` 커밋 + CHANGELOG 통합 정리 + `configure({ transportFactory })` 설계 명세 문서 게시
