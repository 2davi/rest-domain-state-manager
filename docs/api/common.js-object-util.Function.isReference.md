# Function: isReference()

```ts
function isReference(value): boolean;
```

isReference() : 값이 null이 아닌 참조 타입(object | function)인지 확인한다.

## Parameters

### value

`any`

각 유틸 함수의 value 파라미터

## Returns

`boolean`
