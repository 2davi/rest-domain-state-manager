# Interface: GetOptions

## Properties

### urlConfig?

> `optional` **urlConfig?**: [`UrlConfig`](core.url-resolver.Interface.UrlConfig.md)

이 요청에만 적용할 URL 설정 오버라이드.
  미입력 시 `ApiHandler` 인스턴스의 `_urlConfig`가 사용된다.
