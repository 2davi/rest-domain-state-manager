# Function: isPrimitive()

```ts
function isPrimitive(value): boolean;
```

isPrimitive() : 값이 원시 타입(primitive)인지 확인한다.
null 포함.

## Parameters

### value

`any`

각 유틸 함수의 value 파라미터

## Returns

`boolean`
