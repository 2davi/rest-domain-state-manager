# Function: setSilent()

> **setSilent**(`value`): `void`

silent 플래그를 설정한다.
`DomainState.configure({ silent })` 내부에서만 호출되어야 한다.

## Parameters

### value

`boolean`

`true`이면 모든 내부 로그 억제

## Returns

`void`
