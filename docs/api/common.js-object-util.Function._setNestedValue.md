# Function: \_setNestedValue()

> **\_setNestedValue**(`target`, `keys`, `value`): `void`

점(dot) 분해된 키 배열로 중첩 객체에 값을 설정한다.

## Parameters

### target

`object`

### keys

`string`[]

### value

`any`

## Returns

`void`
