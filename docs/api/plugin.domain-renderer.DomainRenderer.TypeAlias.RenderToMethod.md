# Type Alias: RenderToMethod

> **RenderToMethod** = (`container`, `config`) => [`RenderResult`](plugin.domain-renderer.DomainRenderer.TypeAlias.RenderResult.md)

## Type Parameters

## Parameters

### container

`string` \| `HTMLElement`

렌더링 결과를 삽입할 컨테이너. CSS 셀렉터 형식 문자열(`'#id'` 또는 `'id'`) 또는 `HTMLElement`.

### config

[`RenderConfig`](plugin.domain-renderer.DomainRenderer.TypeAlias.RenderConfig.md)

렌더링 설정 옵션

## Returns

[`RenderResult`](plugin.domain-renderer.DomainRenderer.TypeAlias.RenderResult.md)
